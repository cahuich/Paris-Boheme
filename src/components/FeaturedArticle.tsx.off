import React from "react";

export default function FeaturedArticle({ title, slug }: { title: string; slug: string }) {
  return (
    <div>
      <h3>{title}</h3>
      <p>{slug}</p>
    </div>
  );
}
